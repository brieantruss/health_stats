config {
  type: "view",
  schema: "health_stats",
  name: "view_cycling"
}

# cycling

SELECT DISTINCT
    c.*,
    cast(active_time_seconds as int64) / 60 AS active_time_minutes
FROM
    health_stats.cycling AS c
